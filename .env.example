ASPNETCORE_ENVIRONMENT=Development
ASPNETCORE_URLS=http://0.0.0.0:5000

# CORS origins (repeatable: AllowedOrigins__0, AllowedOrigins__1, ...)
AllowedOrigins__0=http://localhost:5173
AllowedOrigins__1=http://127.0.0.1:5173

# Optional: master password encryption for stored secrets
Secrets__MasterPassword=change-me

# Gmail OAuth (set in server environment)
GoogleOAuth__ClientId=your-google-client-id.apps.googleusercontent.com
GoogleOAuth__ClientSecret=your-google-client-secret
GoogleOAuth__RedirectUri=http://localhost:5000/api/oauth/google/callback

# Client dev proxy target (only for docker/local dev)
VITE_API_PROXY_TARGET=http://api:5000

# Client production API base (set this for builds targeting production)
# VITE_API_BASE=https://your-api-host/api
